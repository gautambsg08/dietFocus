currentBuild.displayName = "demo-focus-#"+ displayName.number
pipeline {
   agent any
   tools {
        maven 'Maven 3.6.3'
    }
   }
   stages {
    stage("Maven Build") {
      steps {
        bat "mvn clean package"
      }
    }
   }
}
