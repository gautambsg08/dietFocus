currentBuild.displayName = "demo-focus-#"+ currentBuild.number
pipeline {
   agent any
   tools {
        maven 'Maven 3.6.3'
    }
   stages {
    stage("Maven Build") {
      steps {
        sh "mvn clean package"
      }
    }
   }
}
