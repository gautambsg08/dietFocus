pipeline {
   agent any
   environment {
      PATH = "C:/Users/ganan/scoop/apps/maven/current/bin:$PATH"
   }
   stages {
    stage("Maven Build") {
      steps {
        sh "mvn clean package"
      }
    }
   }
}
